# E0.1 — Production-read and runtime acceptance

Canonical main was independently verified at `ecf52aabae7f5eeb9beb4cbebd46226103482837`
with tree `b19e6ed38850d39df9d7a970fef087fb85439764`. The acceptance code is on the isolated
E0.1 branch and does not modify `services/prospective_shadow/**` or CPI-E1 files.

The reviewed components reused are the GET-only `services.kalshi_account_gateway` client and
strict models, M27F live-read acceptance, M15 production journal and disarmed boundary, M16
canary store/recovery, `services.operations.postgres_stores`, risk reconciliation, and the
effective-dated fee policy in `services.opportunity_engine.fees`.

Focused acceptance passed: 200 tests. Fixture reconciliation covers duplicate and missing
fills, impossible quantities, order/fill and position mismatches, unknown identities, stale
pagination, partial fills, and terminal states. Fixture success is not live-fill evidence.

The current official Kalshi documentation compatibility sweep returned HTTP 200 for all 13
reviewed pages and has digest
`4f4ffb88ba34f11563aab9ba4d3124268498e6952dcb56203e4207015abcccd4`.

Authenticated production reads are `BLOCKED_BY_CREDENTIAL`: no approved read-only key, private
key, or authority attestation was available. PostgreSQL acceptance is now positively proven on
PostgreSQL 18.6 using the canonical stores and migrations in the isolated temporary
`e01_r1a_acceptance` database. The three canonical PostgreSQL tests passed, including
process/thread concurrency, uniqueness, rollback, and recovery. Actual PostgreSQL server
stop/start preserved synthetic partial-fill and terminal states and durable `DISARMED` state.
Current fee policy is compatible, but realized live fees were not validated.

The durable, secret-free receipt is
[`acceptance-receipt.json`](artifacts/e01-production-read-runtime/acceptance-receipt.json).
Its deterministic digest is
`2ffcb64ccc80c2027af3e9690edcaadccd2ffd2f1c9ff42da3dee89d74aaf595`.

Safety result: live mutation count `0`, live orders placed `0`, write credentials installed
`false`, production boundary armed `false`, and `production_influence = 0`. No order endpoint
was invoked. Authenticated live-read acceptance remains separately blocked and out of scope for
R1A. Classification: **PASS — POSTGRESQL/RUNTIME ACCEPTANCE COMPLETE**.
